import React from "react";

const LinkInput = () => {
  return (
    <div>
      <input placeholder="Paste an audio link here" />
      <button>Play</button>
    </div>
  );
};

export default LinkInput;
